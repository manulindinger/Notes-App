# Notes App (C#)

A simple console-based Notes application built with C#.
The app allows users to create, view, and delete notes. 
All notes are automatically saved to a local JSON file (and loaded when the application starts).

---

## Features
## Project Structure
